import 'package:flutter/material.dart';

class ChargerScreen extends StatelessWidget {
  const ChargerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("chargerr Scsreen"),
    );
  }
}
